package main

// Desktop is where Bragi saves the user session to allow rapid reopening.
// Desktop should allow to save opened buffers (reloading the file in theme, if exists)
// it should also save the window position and size, if it's maximized and other
// interesting things that we want to reload every time Bragi is reopened.
